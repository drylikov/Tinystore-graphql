export enum UserErrors {
  INVALID_CONFIRM_TOKEN_OR_EMAIL = 'Email or confirm token are invalid',
  INVALID_LOGIN = 'Invalid email or password',
  CONFIRM_TOKEN_NOT_VALIDATED = 'You need to confirm your account first',
}
